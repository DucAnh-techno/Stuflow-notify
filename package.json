{
  "dependencies": {
    "@types/axios": "^0.9.36",
    "@types/cors": "^2.8.19",
    "@types/dotenv": "^6.1.1",
    "axios": "^1.12.2",
    "cors": "^2.8.5",
    "dotenv": "^17.2.2"
  },
  "scripts": {
    "build": "npm install --prefix front-end && npm run build --prefix front-end"
  }
}
